# HTTP-клиент для общения с локальным LLM-сервером
requests>=2.28.0

# Шаблонизатор Markdown/HTML
Jinja2>=3.1.2

# Python-биндинги для libclang (AST-парсер C++)
clang>=13.0.0

# (Опционально) если собираетесь скачивать модель из HF без git-lfs
huggingface-hub>=0.13.4

# Для модульных тестов
pytest>=7.0.0
